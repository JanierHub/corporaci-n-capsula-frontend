# Opción A (recomendado en local): no definas VITE_API_URL — el front usa `/api/v1` y el proxy de Vite
# reenvía a http://localhost:3000 (levanta el back en el puerto 3000 o el que tengas en PORT).
#
# Opción B: llamada directa al back (necesitas FRONTEND_URL=http://localhost:5173 en el .env del back).
# VITE_API_URL=http://localhost:3000/api/v1